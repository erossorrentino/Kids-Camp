/**
 * Which build this is, shown on the title screen so a player can tell an
 * old copy from a new one. `tools/build-artifact.mjs` stamps it; served
 * straight from source it reads 'dev'.
 */
export const BUILD = 'dev';
